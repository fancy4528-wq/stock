# Copy to .env and fill in local values. Never commit .env.

# Docker / PostgreSQL
PG_PASSWORD=change-me
POSTGRES_DB=quantagent
POSTGRES_USER=quantagent

# Docker / Redis
REDIS_PASSWORD=change-me

# Optional data-source tokens (P0)
TUSHARE_TOKEN=

# Optional LLM (P1+)
LLM_API_KEY=
